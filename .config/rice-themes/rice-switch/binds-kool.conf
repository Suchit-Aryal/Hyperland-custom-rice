# binds-kool.conf — active while the kool rice (waybar + swaync) is running
# Only the rice cycle lives here; all normal keybinds come from Keybinds.conf

bindd = SUPER SHIFT, T, cycle rice (kool -> end4), exec, ~/.config/rice-themes/rice-switch/rice-switch.sh toggle