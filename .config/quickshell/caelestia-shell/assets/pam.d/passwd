#%PAM-1.0
# Caelestia lock screen (user override).
# Stock config required pam_faillock, whose tally dir (/run/faillock)
# does not exist on this system, so EVERY attempt ended as PAM Error
# ("PW ERROR") before the password was even checked.
# This system login (common-auth) uses plain pam_unix, so the locker does too.

auth    [success=1 default=bad]     pam_unix.so         nullok
